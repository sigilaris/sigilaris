# Sigilaris v0.3.0-M2 Release Notes

## Final Status

- Artifact version: `0.3.0-M2`.
- Publication status: `PUBLISHED` on Sonatype Central.
- Deployment id: `344e1f3d-16ab-428d-8e2e-bb7f5d4c134b`.
- Publication confirmed on 2026-09-06 at 09:04:48 KST (00:04:48 UTC).
- Release source: annotated `v0.3.0-M2` at the `--no-ff` main merge
  `cc9a77631f4e988a3f18fbf8983857aef59be302` in the canonical repository.
- Public export tag: `v0.3.0-M2` at snapshot
  `02dd846275b40bd03e31671f6ce1d317a6825077`. The public repository contains
  filtered snapshots, not the canonical private commit history.
- This release supersedes M1 for integrations using the hardening below.
  The published M1 tag and coordinates remain immutable.

## Scope

M2 packages the reviewed post-M1 application-safety and exact-pipeline
hardening. It keeps the M1 protocol versions and the one-producer,
one-consumer exact dependency boundary.

- Admission capacity follows the authoritative application journal and
  conservatively accounts for admission rows not yet journaled. Accepted exact
  work owns drain liveness and advances its deadline watermark.
- Canonical expiry and drain readiness are checked in one application-store
  transaction. Terminal execution identities survive reuse of their old input
  rows, preventing reacquisition of an expired execution.
- Journaled terminal exact history survives verifier-profile retirement.
  New and nonterminal work still requires the active profile. Unchanged
  reconciliation does not create journal entries.
- Coherent legacy drain state is repaired only after journal/physical
  equivalence is proven. The safe-direction repair is itself journaled and
  subsequent restarts are idempotent.
- Exact uniqueness indexes retain identifiers and commitments instead of
  complete payloads. Replay reads immutable primary rows under the store gate.
  Ambiguous primary writes invalidate the derived indexes until restart;
  corrupt/conflicting primary rows fail startup with key diagnostics.
- Exact waits use bounded exponential polling with a five-minute hard cap.
  Operator diagnostics require explicit authenticated endpoint mounting.
- Block views, split header persistence, manifest construction, and exact
  descriptor boundaries reject invalid versioned commitments.

## Migration From M1

Upgrade all five module coordinates together and recompile custom integrations:

- Use `ExactPipelineAdmissionService.create` and provide
  `ApplicationSafetyRuntime`.
- Handle manifest `withComputedDigest` and `computeConfigurationDigest`
  results as `Either`; unsafe construction helpers are for trusted fixtures.
- Handle `BlockStore.putHeader` as `EitherT`.
- Preserve `ApplicationSafetyState.terminalExecutions` and expire through
  `expireExecution`.
- Update exhaustive matches for new application-safety, input-validation,
  and manifest-validation failure cases.
- Implement `ExactTxPipelineStore.getByRequestIdentity` in custom stores and
  update derived ownership indexes consistently with primary records.
- Implement `ExactTxPipelineStore.countUnjournaled(journaledPipelineIds, stopAt)`
  as one bounded primary-key scan that counts non-journaled rows up to
  `stopAt`, returning zero for nonpositive limits. Repeated full-record
  decoding or sorting is not an acceptable admission-capacity implementation.
- Explicitly mount `applicationStatusEndpoint` on an authenticated operator
  plane; it is absent from the default exact endpoint bundle.
- Accommodate the five-minute maximum exact wait, including absent timeouts.
- Reconcile under the current manifest and drain before retiring a profile.
  Preserve terminal exact history across rotation.
- Require canonical 32-byte lowercase verifier-manifest digests. A legacy
  noncanonical configured digest requires drain/rotation using the old binary;
  it cannot be silently rewritten without changing configuration identity.

Back up the complete stopped-node consistency group before the first M2
start: application safety/journal, exact admission/idempotency, generic
pipeline, block, consensus, and state namespaces. Recovery may append a
`repairLegacyDrainState` entry. Physical/journal disagreement still refuses
startup. Restoring one namespace independently is unsupported; retain the
pre-upgrade backup for rollback instead of assuming an older binary can read
the upgraded store.

The repair warning is best-effort and can be lost across a crash before
notification. The journal entry is the authoritative record. Exact `list`
ordering is backend-specific, with no cross-page snapshot guarantee during
writes; startup reconciliation is order-independent and precedes admission.

The public [integration guide](v0.3.0-M2-integration-guide.md) describes the
full registration, submission, drain, and recovery workflow.

## Coordinates

| Platform | Maven coordinate |
| --- | --- |
| JVM core | `org.sigilaris:sigilaris-core_3:0.3.0-M2` |
| Scala.js core | `org.sigilaris:sigilaris-core_sjs1_3:0.3.0-M2` |
| JVM node common | `org.sigilaris:sigilaris-node-common_3:0.3.0-M2` |
| Scala.js node common | `org.sigilaris:sigilaris-node-common_sjs1_3:0.3.0-M2` |
| JVM node runtime | `org.sigilaris:sigilaris-node-jvm_3:0.3.0-M2` |

## Verification

The reviewed implementation at `3f19d96` passed 1,910 source tests:
core JVM 483, core Scala.js 482, node-common JVM 122,
node-common Scala.js 121, and node-jvm 702. The fifth external review
reported no CRITICAL/WARNING and reran the 27 exact-store/admission and
15 application-safety tests successfully. These are the existing source
verification results, not a claim of a new M2 test run.

M2 preparation on 2026-09-06 passed `scalafmtCheckAll`,
`nodeJvm/Test/compile`, `publishM2` (Maven Local staging), and `publishSigned`.
No production behavior changed after the reviewed source gate; the sole
production-source edit documents the existing list-order contract.

The standalone Maven consumer ran on JVM, full-linked on Scala.js, and ran
both the fast and fully optimized JavaScript outputs. It exercises the new
manifest `Either` contract, in-memory exact-store creation, request-identity
lookup, and unjournaled counting. Its npm lock explicitly supplies the
library's JavaScript dependencies.

All five POMs pin Sigilaris dependencies to M2 and contain no snapshot or
local-repository reference. The ten Maven Local POM/main-JAR files match
signed staging byte-for-byte. All 20 staging signatures (POM, main JAR,
sources, Javadoc) verify with fingerprint
`09710A1F5E321DE8829C3F1902F21A461014CD4B`; staging MD5/SHA-1 sidecars also
match. The [SHA-256 manifest](v0.3.0-M2-checksums.sha256) records the ten
verified POM/main-JAR files. All 347 packaged Scala source files across the
five source JARs match the reviewed working tree.

After the `--no-ff` merge, `sonatypeBundleRelease` published that same signed
bundle without rebuilding it. Central confirmed deployment
`344e1f3d-16ab-428d-8e2e-bb7f5d4c134b` as `PUBLISHED`.

All ten public POM/main-JAR files downloaded directly from
`https://repo.maven.apache.org/maven2` match the signed staging copies
byte-for-byte and the SHA-256 manifest. All ten public detached signatures
verify with the release fingerprint above.

An independent Coursier fetch resolved all five coordinates and their
transitive dependencies into a fresh cache (159 JARs), using
`env -u COURSIER_REPOSITORIES cs fetch --no-default -r https://repo.maven.apache.org/maven2`
with that cache explicitly selected. No Maven Local/default resolver was
available. The five cached POMs and five main JARs separately passed all ten
SHA-256 manifest checks.

## Release Review And Lessons

- Review found a missing `countUnjournaled` migration instruction and an
  overstated verifier-slot uniqueness requirement; both guides now match the
  public APIs and pair-based registry validation.
- The first expanded Scala.js consumer exposed missing npm dependencies even
  on a rejected manifest path. Pinning those dependencies and executing both
  linker outputs made the standalone consumer representative of a real
  downstream build. The JVM consumer runs in a fork for correct IOApp cleanup.
- The integration guide lives in `docs/releases` because public snapshot
  export excludes `docs/dev`; published migration instructions must be
  present in the exported tree.
- Export the release tag before pushing the merged main ref and wait for
  that export to finish: the current export workflow cancels concurrent
  runs and skips tag creation for an unchanged snapshot. Both M2 tag and
  main export runs completed successfully, preserving the public tag.
- Include a dependency-advisory review before the artifact freeze in future
  releases. Release-time alerts exposed inherited Scala.js dependencies;
  passing functional and artifact-integrity gates does not resolve those
  security advisories.
- Remaining code changes are tracked in proposed plan 0032. They do not enter
  the M2 artifact freeze. Subsequent releases should retain explicit npm
  setup, complete abstract-store API migrations, public documentation checks,
  and signed/local/public artifact comparisons.
- Final preparation review: `No finding`. New public-document relative links
  and `git diff --check` passed; the reviewed runtime behavior is unchanged.
- Final publication-document review: `No finding`. The nine relative links
  in the three final documentation changes, including both direct security
  anchors, resolve and survive public export. Only documentation changed
  after the immutable release tag; artifact sources and dependency locks
  remain unchanged.

## Follow-up Work

### Dependency Security Follow-up

**M1/M2 JVM consumers also retain `bcprov-jdk15on 1.70`.** The
[2026-09-13 dependency audit](../conformance/m3-crypto-dependency-audit-2026-09-13.json)
matched five advisories for that provider version. The immutable legacy JVM
conformance profiles deliberately retain it; passing those profiles does not
remediate the provider or establish each advisory's deployment applicability.
The M3 move to `bcprov-jdk18on 1.86` does not retroactively patch M1/M2.

During the release push, GitHub dependency alerts identified the inherited
Scala.js runtime dependency `elliptic 6.5.4` as affected by the Critical
[GHSA-vjh7-7g9h-fjfh](https://github.com/advisories/GHSA-vjh7-7g9h-fjfh).
The advisory affects versions through `6.6.0`; its first patched version is
`6.6.1`. The library dependency and existing yarn locks are unchanged from
M1; the new standalone smoke lock makes the same version explicit.

This dependency is not test-only: the Scala.js facade imports `elliptic`
at runtime. Sigilaris converts signing inputs from `Array[Byte]` to
`Uint8Array`, while the advisory describes malformed-input signing. This
limited release check does not establish exploitability or prove the path
unaffected. M2 does not fix this advisory, and the application-safety review's
`No finding` is not a dependency-security clearance.

Prioritize a separate security remediation review before production signing:
assess all runtime crypto advisories, select a supported dependency update,
and verify JVM/Scala.js signature and recovery compatibility plus downstream
consumers. Audit the existing webpack/dev-server dependency alerts separately
as build/development tooling exposure. Publish any artifact changes under new
coordinates; do not overwrite M2. This security work is separate from the
non-blocking operational suggestions in plan 0032.

The [M3 crypto security record](../conformance/m3-crypto-security-2026-09-13.md)
tracks the later runtime remediation under new M3 candidate coordinates,
including remaining advisory assessments. M2 artifacts and its original smoke
runtime remain unchanged. The broader Scala.js build-tool findings are tracked
in [plan 0035](../plans/0035-scala-js-build-dependency-security-plan.md).

### Operational Hardening

The non-blocking fifth-review suggestions are tracked in
[plan 0032](../plans/0032-application-safety-and-exact-pipeline-operational-hardening-plan.md):
observer exception isolation, typed alias-write failures, synchronized index
rebuild, corruption diagnostics, and targeted regression gaps. The plan is
proposed work after M2, not part of this release.

Checkpointed journal retention, a single-namespace exact transaction,
event-driven waits, and explicit V2 cutover wiring remain separate follow-up
work. M2 does not expand the exact protocol to arbitrary DAGs, multiple edges,
cross-pipeline or cross-epoch consumption, or manual lock release.
